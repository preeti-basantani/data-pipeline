# data-pipeline
 a robust ETL pipeline that consumes Kafka events and nightly batch loads to a Delta Lake / S3 data lake with schema evolution handling. Tech: Kafka, PySpark (batch + streaming), Delta Lake / Parquet on S3 (or local), Airflow or cron for orchestration. 
